import 'package:flutter/material.dart';

void main() {
  runApp(MaterialApp(
    home: Scaffold(
      appBar: AppBar(

    title : Text("whatsapp",style: TextStyle(color: Colors.red,fontSize: 40),),
    backgroundColor: Colors.teal,
    
      ),
body: GridView.count(
  crossAxisCount:2,
  children: [
        Container(
          height: 200,
          width: 200,
          color: Colors.blue,
        ),

       Container(
        height: 200,
        width: 200,
        color: Colors.black26,
       ) 
  ],
  
  
  ),






    ),
  ));
}
